# 酒店退款 Agent MVP｜前端整体设计方案

## 1. 产品叙事

整个前端只保留三个一级页面，顺序本身就是作品的讲述逻辑：

1. **Agent 设计**：先让面试官在 30 秒内理解系统如何工作。
2. **场景模拟**：再让面试官亲手验证不同业务路径。
3. **运营看板**：最后证明产品如何被衡量，而不只是一段能对话的 Demo。

对应的认知路径是：**看懂设计 → 体验能力 → 验证价值**。

不设置独立首页、功能总览、关于项目、帮助中心、设置页或多级侧边栏。Agent 设计页就是默认首页。

---

## 2. 全局框架

### 顶部栏

只包含：

- 产品名：酒店退款 Agent MVP
- 一级导航：Agent 设计／场景模拟／运营看板
- `Mock 数据`标识

不放搜索、消息、头像、设置、面包屑等与演示目标无关的元素。

### 导航原则

- 打开链接默认进入 `/agent-design`。
- 每个页面只有一个主目标。
- 导航始终使用同一顺序，形成完整讲述路径。
- 页面底部可以出现“下一步”按钮，但不额外复制一套导航。

---

## 3. 页面一：Agent 设计

### 唯一目的

让面试官一眼理解：这个 Agent 如何从用户问题走到真实业务结果，以及 LLM、Tool、RAG、规则、记忆和人工分别负责什么。

### 首屏结构

1. 标题：酒店退款 Agent 如何完成一次售后任务
2. 一句话定义：LLM 负责理解与表达，规则和 Tool 决定并推进真实业务状态。
3. 简化流程图：一条主链路，四个业务出口。
4. 唯一主按钮：`开始场景模拟`

### 流程图主链路

```text
用户问题
  ↓
LLM 理解诉求与风险
  ↓
Tool 确认订单并读取事实
  ↓
规则引擎检查完整度并分流
  ↓
自动退款／退款查询／供应商协商／紧急转人工
  ↓
回复用户并写入 Case Memory
```

### 节点表达规范

每个节点只展示两行：

- 第一行：业务动作，例如“确认订单与事实”。
- 第二行：实现机制，例如“订单 Tool + 身份校验”。

技术机制不单独做成另一个架构图，避免面试官在两张图之间来回映射。

### 次级信息

流程图下方只保留三条设计边界：

- LLM 不计算退款金额，也不决定操作权限。
- 写操作必须经过用户二次确认和幂等校验。
- 高风险、低置信度和异常履约场景完整转人工。

---

## 4. 页面二：场景模拟

### 唯一目的

让面试官亲自验证 Agent 在不同酒店退款任务中的判断、行动和预期管理。

### 场景数量

只提供三个代表性场景，覆盖三类不同 Agent 能力：

1. **免费取消**：政策明确，Agent 自动报价、二次确认并执行。
2. **退款进度**：不重复发起退款，读取资金状态并解释预计到账。
3. **到店无房**：目标从“退款”切换为“先恢复今晚住宿”，并进入人工协同。

不增加更多相似场景。三个场景已经能分别展示自动执行、状态查询和紧急兜底。

### 页面布局

- 顶部：三个场景切换项，附一句“本场景验证什么”。
- 左侧约 65%：真实 C 端用户看到的交互界面。
- 右侧约 35%：面试演示专用的 Agent 决策轨迹。

### 决策轨迹只展示

- 当前 Workflow 节点
- 使用的事实和 Tool
- 命中的规则或安全机制
- 下一步动作

不展示完整 Chain of Thought、模型隐性推理或冗长日志。

### 交互原则

- 点击场景后直接载入一个可继续操作的初始状态。
- 每一步只提供一个明显主操作。
- 用户侧不出现 LLM、RAG、Prompt 等术语。
- 右侧轨迹随用户操作更新，用于证明 Agent 的行为是可解释、可审计的。
- 页面顶部持续显示“模拟订单与政策”，避免作品被误解为去哪儿真实系统。

---

## 5. 页面三：运营看板

### 唯一目的

清楚回答：这个智能客服是否真正解决了用户问题，而不是只完成了一次回复。

### 北极星指标

**有效自助解决率**

```text
有效自助解决率 =
Agent 独立推动业务状态成功完成，且 72 小时内未转人工、未因同一问题重复进线的会话数
÷
进入 Agent 且属于 MVP 可处理范围的有效会话数
```

“回复完成”不能算解决；必须至少满足订单、退款单、供应商工单或住宿恢复状态中的一项被真实推进。

### 看板信息顺序

1. 北极星指标当前值、口径和数据周期。
2. 近 8 周趋势，判断产品是否持续改善。
3. 按三个场景拆解，定位哪类任务拖累整体结果。
4. 两个护栏指标：错误执行率、72 小时重复进线率。
5. 一条“本周最需解决的问题”，把数据转成产品行动。

### 为什么不展示更多指标

平均轮次、首响时间、Token 成本、满意度等可作为诊断数据，但首版不进入主看板。它们容易稀释“是否解决问题”这一核心判断。

### Mock 数据规范

- 页面明显标注“演示数据”。
- 所有数字保持内部逻辑一致，但不声明来自去哪儿。
- 指标旁可以展开查看口径，避免只展示漂亮数字。

---

## 6. 页面关系

```text
打开分享链接
  ↓
Agent 设计：看懂系统
  ↓ 点击“开始场景模拟”
场景模拟：验证三类能力
  ↓ 点击“查看运营结果”
运营看板：验证是否创造业务价值
```

三页可以通过顶部导航自由跳转，但推荐路径保持线性。

---

## 7. 需要删除的冗余设计

- 删除独立的 Demo 欢迎页，Agent 设计页承担入口作用。
- 删除功能卡片墙，不罗列“对话、知识库、工具、记忆”等能力。
- 删除传统后台侧边栏，一级导航只有三个。
- 删除无业务意义的 AI 动效、模型参数、Prompt 编辑器和技术日志。
- 删除多张 KPI 卡片，运营看板只突出一个北极星指标和两个护栏指标。
- 删除与求职作品叙事无关的账号、通知、设置和帮助模块。

---

## 8. 面试官应获得的最终印象

- 你不是在做一个聊天机器人，而是在设计一套能推进业务状态的 Agent。
- 你知道 LLM 的能力边界，也知道何时使用规则、Tool、记忆和人工。
- 你能从真实用户任务出发设计界面，而不是为展示技术堆功能。
- 你能用业务结果定义北极星指标，并通过护栏指标控制自动化风险。
